/**
 * All Global prefs should be in this file
 * This is different from the config files since this is constant regardless of ENV settings
 */
export const GlobalPrefs = {
    pagination: {
        limit: 30
    }
}
